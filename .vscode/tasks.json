{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "java (buildArtifact)",
            "mainClass": "ru.gavrikov.Main",
            "targetPath": "${workspaceFolder}/${workspaceFolderBasename}.jar",
            "elements": [
                "${compileOutput}",
                "${dependencies}"
            ],
            "problemMatcher": [],
            "label": "java (buildArtifact): ApkRenamer",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}